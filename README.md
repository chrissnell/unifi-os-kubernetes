# Helm chart repository

This branch is auto-managed by [chart-releaser-action](https://github.com/helm/chart-releaser-action). It hosts the packaged Helm chart for [`unifi-os-kubernetes`](https://github.com/chrissnell/unifi-os-kubernetes).

```bash
helm repo add unifi-os https://chrissnell.github.io/unifi-os-kubernetes
helm install unifi unifi-os/unifi-os-server
```
